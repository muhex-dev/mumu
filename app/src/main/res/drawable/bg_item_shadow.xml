<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Shadow (Blurred effect simulated by offset) -->
    <item
        android:bottom="0dp"
        android:left="2dp"
        android:right="0dp"
        android:top="2dp">
        <shape android:shape="rectangle">
            <solid android:color="#33000000" />
            <corners android:radius="14dp" />
        </shape>
    </item>

    <!-- Main Background -->
    <item
        android:bottom="2dp"
        android:left="0dp"
        android:right="2dp"
        android:top="0dp">
        <shape android:shape="rectangle">
            <solid android:color="@color/adap" />
            <stroke
                android:width="1dp"
                android:color="@color/adapt" />
            <corners android:radius="14dp" />
        </shape>
    </item>
</layer-list>
